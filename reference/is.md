# Check Objects

Check if the argument is a valid object.

## Usage

``` r
is.polm(x)

is.lpolm(x)

is.lmfd(x)

is.rmfd(x)

is.stsp(x)

is.pseries(x)

is.zvalues(x)
```

## Arguments

- x:

  object

## Value

`TRUE` if the argument is a valid object.
